<!DOCTYPE html>
<html>
	<head>
		<title>Main</title>
		<meta charset="UTF-8"/>
	</head>
	<body>
		<h1 align = "center"> 添加课堂任务 </h1>
		<div align="center">
		    <% flash.each do |name, msg| -%>
              <%= content_tag :div, msg, class: name %>
           <% end -%>
		    
    	<%= form_for :assignment, :method => 'post', :url => {:action => 'add_assignment'} do |f| %>
     <p>
    题目描述(选择题)：<%= f.text_field :content %>
    </p>
    <p>
    题目答案(答案序列)：<%= f.text_field :answer %>
    </p>
  	<p><%= submit_tag "添加" %></p>
	     <% end %>
		</div>
		
	</body>
</html>
